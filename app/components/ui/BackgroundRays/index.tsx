import styles from './styles.module.scss';

const BackgroundRays = () => {
  return (
    <div className={styles.rayContainer}>
      {/* Simple solid color background - no need for individual rays */}
    </div>
  );
};

export default BackgroundRays;
